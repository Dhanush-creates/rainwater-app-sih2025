// Fill in your API keys here or provide via environment variables.
// For reference, see `api/config.example.js`.

// API Keys - use hardcoded values for now to avoid build issues
// AccuWeather API key has expired - app will use fallback services
export const ACCUWEATHER_API_KEY = null;
export const GEMINI_API_KEY = "";
export const GROK_API_KEY = null; // Key stored in Vercel environment variables

export const DEFAULT_WRIS = {
  stateCode: null,
  districtCode: null,
};

export const GEMINI_MODEL = "gemini-1.5-flash";

