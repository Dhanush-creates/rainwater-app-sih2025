// API configuration.
// All API keys are now stored securely in Vercel environment variables (server-side).
// This file is kept for backward compatibility only — no keys should be set here.
// See env.example for the list of required environment variables to set in Vercel dashboard.

export const ACCUWEATHER_API_KEY = null;
export const GEMINI_API_KEY = null;
export const GROK_API_KEY = null;

export const DEFAULT_WRIS = {
  stateCode: null,
  districtCode: null,
};

export const GEMINI_MODEL = "gemini-2.5-flash";
